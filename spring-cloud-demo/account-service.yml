server:
    port: 9030

logging:
  level:
    org.springframework: INFO

#maybe will need change this value on default=30 in production
eureka:
  instance:
    lease-renewal-interval-in-seconds: 10
    prefer-ip-address: true

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 10000

eureka:
  client:
    serviceUrl:
      defaultZone: http://registry:8771/eureka/

spring:
  rabbitmq:
    host: rabbitmq

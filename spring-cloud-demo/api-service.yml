server:
    port: 9010

logging:
  level:
    org.springframework: DEBUG

account-service:
  ribbon:
    listOfServers: localhost:9020

eureka:
  client:
    enabled: false

ribbon:
  eureka:
    enabled: false

feign:
  hystrix:
    enabled: false